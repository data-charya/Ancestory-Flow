// Tree lines disabled - using layout and labels instead
// The hierarchical layout already shows relationships visually

export function useTreeLines() {
  return []; // No lines - cleaner appearance
}
